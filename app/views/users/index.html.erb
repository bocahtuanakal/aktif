<% provide(:title, "User Lisy") %>
<h1>Users of Get Active</h1>

<%= will_paginate %>

<ul class="users">
    <% @users.each do |user| %>
        <%= render @users %>
    <% end %>
</ul>

<%= will_paginate %>
